apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
    - host: ingress.${domain}
      http:
        paths:
          - backend:
              serviceName: web-front-1
              servicePort: 80
            path: /
